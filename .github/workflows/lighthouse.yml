name: Lighthouse CI

on:
  pull_request:
    branches:
      - main
    types: [opened, edited, reopened, synchronize]

jobs:
  lighthouse-audit:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3

      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: 16

      - name: Install dependencies
        run: |
          npm install -g @lhci/cli@0.11.x serve

      - name: Start local server
        run: |
          serve public &
          # Save the PID to kill the server later
          echo $! > .server-pid

      - name: Wait for server to start
        run: sleep 5

      - name: Run Lighthouse audit
        id: lighthouse
        run: |
          REPORTS_DIR="lighthouse-reports"
          mkdir -p $REPORTS_DIR

          # Find all HTML files in the public folder
          HTML_FILES=$(find public -name "*.html")

          # Initialize the markdown table
          echo "| File name | Performance | Accessibility | Best practices | SEO |" > $REPORTS_DIR/summary.md
          echo "| --------- | ----------- | ------------- | -------------- | --- |" >> $REPORTS_DIR/summary.md

          # Process each HTML file
          for file in $HTML_FILES; do
            # Get relative path to display in report
            RELATIVE_PATH=$(echo $file | sed 's|^public/||')

            # Create a unique report directory for each file
            FILENAME=$(basename "$file" .html)
            FILE_REPORT_DIR="$REPORTS_DIR/$FILENAME"
            mkdir -p "$FILE_REPORT_DIR"

            # Run Lighthouse
            URL="http://localhost:3000/$(echo $file | sed 's|^public/||')"
            lhci collect --url="$URL" --output="json" --outputDir="$FILE_REPORT_DIR"

            # Extract scores from the latest report
            LATEST_REPORT=$(ls -t $FILE_REPORT_DIR/lhr-*.json | head -n 1)

            # Extract scores using jq
            PERFORMANCE=$(cat $LATEST_REPORT | jq '.categories.performance.score * 100' | xargs printf "%.0f")
            ACCESSIBILITY=$(cat $LATEST_REPORT | jq '.categories.accessibility.score * 100' | xargs printf "%.0f")
            BEST_PRACTICES=$(cat $LATEST_REPORT | jq '.categories["best-practices"].score * 100' | xargs printf "%.0f")
            SEO=$(cat $LATEST_REPORT | jq '.categories.seo.score * 100' | xargs printf "%.0f")

            # Add to markdown table
            echo "| $RELATIVE_PATH | $PERFORMANCE | $ACCESSIBILITY | $BEST_PRACTICES | $SEO |" >> $REPORTS_DIR/summary.md
          done

          # Save the markdown content to a variable for the PR comment
          SUMMARY_CONTENT=$(cat $REPORTS_DIR/summary.md)
          echo "summary<<EOF" >> $GITHUB_ENV
          echo "$SUMMARY_CONTENT" >> $GITHUB_ENV
          echo "EOF" >> $GITHUB_ENV

      - name: Kill server
        if: always()
        run: |
          if [ -f .server-pid ]; then
            kill $(cat .server-pid)
          fi

      - name: Comment on PR
        uses: actions/github-script@v6
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
          script: |
            const fs = require('fs');

            const summary = process.env.summary;

            github.rest.issues.createComment({
              issue_number: context.issue.number,
              owner: context.repo.owner,
              repo: context.repo.repo,
              body: `## 📊 Lighthouse Audit Results\n\n${summary}\n\n_This comment was automatically generated from Lighthouse audit on \`public\` folder HTML files._`
            });
